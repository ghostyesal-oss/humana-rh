# Dumps Postgres (ne jamais committer une base RH)
bd_dump
*.dump
dumps/
auth-users.sql
data-public.sql
data-storage.sql
schema-public.sql
schema-storage.sql

# Environnement local / secrets
.env
.env.local
.env.*.local
deploy/.env
*.pem
*.key
*.p12
*.pfx

# config.js est généré en prod (generate-config.mjs) et recopié en local
# depuis config.example.js. Ne pas le versionner.
config.js
config.local.js

# Sortie locale des scans Snyk
.snyk-out/

# Cles VAPID / service role (ne doivent JAMAIS partir sur git)
vapid-*.txt
service-role.txt
*-service-role.txt

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editeurs
.vscode/
.idea/
*.swp
*.swo
*.sublime-workspace

# Sauvegardes locales (produites par scripts/extract-pages.mjs, etc.)
*.bak

# Sauvegardes et archives (données personnelles : ne jamais commiter).
backups/
humana_[0-9]*/
humana_[0-9]*.7z
*.dump

# OS
Thumbs.db
.DS_Store
desktop.ini

# Build / cache
dist/
build/
.cache/
.parcel-cache/

# Ancien CLI (dossiers temporaires)
supabase/.temp/
supabase/.branches/
